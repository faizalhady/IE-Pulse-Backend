# MES Endpoint Contract — completion-status pipeline

Generated by `scripts/test_apis.py` on 2026-07-28 09:46. Re-run this before any large pull; do not edit by hand.

| Endpoint | Id param | Verdict | OK |
|---|---|---|---|
| `Customer/ListCustomer` | `none (partialKey)` | 109 active customers; Customer_ID is the id space for #21/#132 | PASS |
| `Assembly/ListAssembly` | `custId` | FAILED — Assembly/ListAssembly failed: Procedure or function 'up_CR_ListAssemblyByCustomerAndActive | FAIL |
| `#21 Batch/ListBatchCountsByRouteStep` | `CustomerID` | per-customer, 1-day window; bogus=0 rows | PASS |
| `#126 Test/ListTestDataWithinTime` | `none — time window only` | 4827 rows/30min, 23 customers; 45min=rejected | PASS |
| `#132 Wip/BoardHistoryReport` | `custId + serial` | 3/4 probe serials reached a final step; bogus=0 | PASS |
| `Route/* (2-call chain)` | `assemId / fmaRouteId` | SKIPPED (--quick) | PASS |

## Notes

- **Customer/ListCustomer** — blank partialKey is accepted here (unlike fmaRoute/step which need '%')
- **#21 Batch/ListBatchCountsByRouteStep** — MUST be called per customer per day. A site-wide call is the 2026-07-24 bug (29 assemblies for the whole site over 120 days).
- **#126 Test/ListTestDataWithinTime** — TEST STEPS ONLY (PACKOUT present=False). Models that never hit a test station get NO serial and must fall back to #21. 30-min window is a hard cap.
- **#132 Wip/BoardHistoryReport** — Completion is proven HERE, not by #126. finished = journey reaches PACKOUT/PACK OUT/OQA/OBA/SHIP. Retry loop needs a cap — an intermittent 404 hung the 2026-07-24 run for 3 hours.

## Pull order

Every MES pull loops workcells **smallest first** (`workcell_map.parquet.pull_order`). Small workcells surface a broken param in seconds; the giants go last so an interruption never costs the whole run.
